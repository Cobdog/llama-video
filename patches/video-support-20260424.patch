diff --git a/tools/mtmd/clip-impl.h b/tools/mtmd/clip-impl.h
index 7d6484e..6469777 100644
--- a/tools/mtmd/clip-impl.h
+++ b/tools/mtmd/clip-impl.h
@@ -461,6 +461,7 @@ struct clip_image_u8_batch {
 struct clip_image_f32_batch {
     std::vector<clip_image_f32_ptr> entries;
     bool is_audio = false;
+    bool is_video = false;  // true if entries are 6-channel video super-frames
 
     // for llava-uhd style models, we need to know the grid size
     // note: entries.size() == grid_x * grid_y + 1 (one overview image)
@@ -471,6 +472,7 @@ struct clip_image_f32_batch {
         clip_image_f32_batch new_batch{
             /* entries  */ {},
             /* is_audio */ is_audio,
+            /* is_video */ is_video,
             /* grid_x   */ grid_x,
             /* grid_y   */ grid_y,
         };
diff --git a/tools/mtmd/clip.cpp b/tools/mtmd/clip.cpp
index 45e3989..21f5c5a 100644
--- a/tools/mtmd/clip.cpp
+++ b/tools/mtmd/clip.cpp
@@ -2845,6 +2845,33 @@ size_t clip_embd_nbytes_by_img(const struct clip_ctx * ctx, int img_w, int img_h
     return clip_n_output_tokens(ctx, &img) * clip_n_mmproj_embd(ctx) * sizeof(float);
 }
 
+bool clip_image_preprocess_video_superframe(
+        struct clip_ctx * ctx,
+        const float * chw_data,
+        int width,
+        int height,
+        struct clip_image_f32_batch * res_imgs) {
+    (void)ctx;
+    const int n_channels = 6;
+    const int n_pixels = width * height;
+
+    clip_image_f32_ptr img_f32(clip_image_f32_init());
+    img_f32->nx = width;
+    img_f32->ny = height;
+    img_f32->buf.resize(n_pixels * n_channels);
+
+    // Convert from CHW (Python/numpy) to HWC-interleaved (clip_image_f32 convention)
+    for (int i = 0; i < n_pixels; i++) {
+        for (int c = 0; c < n_channels; c++) {
+            img_f32->buf[i * n_channels + c] = chw_data[c * n_pixels + i];
+        }
+    }
+
+    res_imgs->entries.push_back(std::move(img_f32));
+    res_imgs->is_video = true;
+    return true;
+}
+
 int32_t clip_get_image_size(const struct clip_ctx * ctx) {
     return ctx->model.hparams.image_size;
 }
@@ -3182,37 +3209,32 @@ bool clip_image_batch_encode(clip_ctx * ctx, const int n_threads, const clip_ima
 
     // set input pixel values
     if (!imgs.is_audio) {
+        // Detect channel count from actual data: 6 for video super-frames, 3 for images
+        const auto & first_img = *imgs.entries[0];
+        const int n_channels = (first_img.buf.size() == (size_t)(first_img.nx * first_img.ny * 6)) ? 6 : 3;
         size_t nelem = 0;
         for (const auto & img : imgs.entries) {
-            nelem += img->nx * img->ny * 3;
+            nelem += img->nx * img->ny * n_channels;
         }
         std::vector<float> inp_raw(nelem);
 
-        // layout of data (note: the channel dim is unrolled to better visualize the layout):
-        //
-        // ┌──W──┐
-        // │     H │  channel = R
-        // ├─────┤ │
-        // │     H │  channel = G
-        // ├─────┤ │
-        // │     H │  channel = B
-        // └─────┘ │
-        //   ──────┘ x B
-
+        // Transpose from HWC-interleaved (clip_image_f32.buf) to CHW-planar (GGML tensor)
+        // For images: 3 channels (R, G, B)
+        // For video super-frames: 6 channels (R1, G1, B1, R2, G2, B2)
         for (size_t i = 0; i < imgs.entries.size(); i++) {
             const int nx = imgs.entries[i]->nx;
             const int ny = imgs.entries[i]->ny;
             const int n = nx * ny;
 
             for (int b = 0; b < batch_size; b++) {
-                float * batch_entry = inp_raw.data() + b * (3*n);
+                float * batch_entry = inp_raw.data() + b * (n_channels * n);
                 for (int y = 0; y < ny; y++) {
                     for (int x = 0; x < nx; x++) {
-                        size_t base_src = 3*(y * nx + x); // idx of the first channel
-                        size_t base_dst =    y * nx + x;  // idx of the first channel
-                        batch_entry[      base_dst] = imgs.entries[b]->buf[base_src    ];
-                        batch_entry[1*n + base_dst] = imgs.entries[b]->buf[base_src + 1];
-                        batch_entry[2*n + base_dst] = imgs.entries[b]->buf[base_src + 2];
+                        size_t base_src = n_channels * (y * nx + x);
+                        size_t base_dst =              y * nx + x;
+                        for (int c = 0; c < n_channels; c++) {
+                            batch_entry[c * n + base_dst] = imgs.entries[b]->buf[base_src + c];
+                        }
                     }
                 }
             }
diff --git a/tools/mtmd/clip.h b/tools/mtmd/clip.h
index a859b38..9d4fb7d 100644
--- a/tools/mtmd/clip.h
+++ b/tools/mtmd/clip.h
@@ -97,6 +97,14 @@ struct clip_image_f32 * clip_image_f32_get_img(const struct clip_image_f32_batch
  */
 void clip_build_img_from_pixels(const unsigned char * rgb_pixels, int nx, int ny, struct clip_image_u8 * img);
 
+/** preprocess a video super-frame: accepts pre-processed CHW float data, converts to internal format */
+bool clip_image_preprocess_video_superframe(
+    struct clip_ctx * ctx,
+    const float * chw_data,   /* 6*H*W floats in channel-first [C,H,W] order */
+    int width,
+    int height,
+    struct clip_image_f32_batch * res_imgs);
+
 struct ggml_tensor * clip_get_newline_tensor(const struct clip_ctx * ctx);
 
 bool clip_image_encode      (struct clip_ctx * ctx, int n_threads, struct clip_image_f32 * img, float * vec);
diff --git a/tools/mtmd/models/qwen3vl.cpp b/tools/mtmd/models/qwen3vl.cpp
index fa1100d..f86a472 100644
--- a/tools/mtmd/models/qwen3vl.cpp
+++ b/tools/mtmd/models/qwen3vl.cpp
@@ -13,16 +13,47 @@ ggml_cgraph * clip_graph_qwen3vl::build() {
 
     int mrope_sections[4] = {d_head/4, d_head/4, d_head/4, d_head/4};
 
-    ggml_tensor * inp_raw = build_inp_raw();
-    ggml_tensor * inp = ggml_conv_2d(ctx0, model.patch_embeddings_0, inp_raw, patch_size, patch_size, 0, 0, 1, 1);
+    const bool is_video_input = (img.buf.size() == (size_t)(img.nx * img.ny * 6));
+    const int n_channels      = is_video_input ? 6 : 3;
+
+    ggml_tensor * inp_raw = build_inp_raw(n_channels);
 
     GGML_ASSERT(img.nx % (patch_size * 2) == 0);
     GGML_ASSERT(img.ny % (patch_size * 2) == 0);
 
-    // second conv dimension
-    {
-        auto inp_1 = ggml_conv_2d(ctx0, model.patch_embeddings_1, inp_raw, patch_size, patch_size, 0, 0, 1, 1);
+    ggml_tensor * inp;
+    if (is_video_input) {
+        // Video: split 6-channel super-frame into two 3-channel frames
+        // inp_raw shape: [nx, ny, 6] — channels are the outermost (slowest) dimension
+        // frame_a = channels 0-2 (offset 0), frame_b = channels 3-5
+        const size_t channel_plane_bytes = ggml_row_size(inp_raw->type, img.nx) * img.ny;
+        ggml_tensor * frame_a = ggml_view_3d(ctx0, inp_raw,
+            img.nx, img.ny, 3,
+            ggml_row_size(inp_raw->type, img.nx),  // nb1: stride between rows
+            channel_plane_bytes,                     // nb2: stride between channels
+            0);                                      // offset: start of channels 0-2
+        ggml_tensor * frame_b = ggml_view_3d(ctx0, inp_raw,
+            img.nx, img.ny, 3,
+            ggml_row_size(inp_raw->type, img.nx),
+            channel_plane_bytes,
+            channel_plane_bytes * 3);                // offset: start of channels 3-5
+
+        // Conv2D(frame_a, weight_t0) + Conv2D(frame_b, weight_t1) = Conv3D decomposition
+        inp = ggml_conv_2d(ctx0, model.patch_embeddings_0, frame_a,
+                           patch_size, patch_size, 0, 0, 1, 1);
+        auto inp_1 = ggml_conv_2d(ctx0, model.patch_embeddings_1, frame_b,
+                                  patch_size, patch_size, 0, 0, 1, 1);
+        inp = ggml_add(ctx0, inp, inp_1);
+    } else {
+        // Image: both Conv2D kernels applied to same 3-channel input (temporal=1)
+        inp = ggml_conv_2d(ctx0, model.patch_embeddings_0, inp_raw,
+                           patch_size, patch_size, 0, 0, 1, 1);
+        auto inp_1 = ggml_conv_2d(ctx0, model.patch_embeddings_1, inp_raw,
+                                  patch_size, patch_size, 0, 0, 1, 1);
         inp = ggml_add(ctx0, inp, inp_1);
+    }
+
+    {
 
         inp = ggml_permute(ctx0, inp, 1, 2, 0, 3);  // [w, h, c, b] -> [c, w, h, b]
         inp = ggml_cont_4d(
diff --git a/tools/mtmd/mtmd-helper.cpp b/tools/mtmd/mtmd-helper.cpp
index 4094074..6e91ff3 100644
--- a/tools/mtmd/mtmd-helper.cpp
+++ b/tools/mtmd/mtmd-helper.cpp
@@ -244,9 +244,11 @@ int32_t mtmd_helper_decode_image_chunk(
         llama_pos * new_n_past) {
     GGML_ASSERT(n_batch > 0);
     auto chunk_type = mtmd_input_chunk_get_type(chunk);
-    const char * name = chunk_type == MTMD_INPUT_CHUNK_TYPE_IMAGE ? "image" : "audio";
+    const char * name = chunk_type == MTMD_INPUT_CHUNK_TYPE_IMAGE ? "image"
+                      : chunk_type == MTMD_INPUT_CHUNK_TYPE_VIDEO ? "video"
+                      : "audio";
     if (chunk_type == MTMD_INPUT_CHUNK_TYPE_TEXT) {
-        LOG_ERR("failed to decode chunk: input chunk not of image/audio type\n");
+        LOG_ERR("failed to decode chunk: input chunk not of image/video/audio type\n");
         return -1;
     }
 
@@ -260,14 +262,15 @@ int32_t mtmd_helper_decode_image_chunk(
     decode_embd_batch batch_embd(encoded_embd, n_tokens, n_pos_per_embd, n_mmproj_embd);
 
     if (mtmd_decode_use_mrope(ctx)) {
-        if (chunk_type == MTMD_INPUT_CHUNK_TYPE_IMAGE) {
+        if (chunk_type == MTMD_INPUT_CHUNK_TYPE_IMAGE
+                || chunk_type == MTMD_INPUT_CHUNK_TYPE_VIDEO) {
             const auto image_tokens = mtmd_input_chunk_get_tokens_image(chunk);
             if (!image_tokens) {
-                LOG_ERR("failed to decode chunk: image tokens are null\n");
+                LOG_ERR("failed to decode chunk: image/video tokens are null\n");
                 return -1;
             }
-            const auto n_tokens = mtmd_image_tokens_get_n_tokens(image_tokens);
-            std::vector<mtmd_decoder_pos> rel_pos(n_tokens);
+            const auto n_tokens_view = mtmd_image_tokens_get_n_tokens(image_tokens);
+            std::vector<mtmd_decoder_pos> rel_pos(n_tokens_view);
             mtmd_helper_image_get_decoder_pos(image_tokens, n_past, rel_pos.data());
             batch_embd.set_position_mrope_2d(rel_pos, seq_id);
         } else if (chunk_type == MTMD_INPUT_CHUNK_TYPE_AUDIO) {
@@ -357,8 +360,12 @@ int32_t mtmd_helper_eval_chunk_single(mtmd_context * ctx,
             *new_n_past += text_batch.n_tokens;
         }
 
-    } else if (chunk_type == MTMD_INPUT_CHUNK_TYPE_IMAGE || chunk_type == MTMD_INPUT_CHUNK_TYPE_AUDIO) {
-        const char * name = chunk_type == MTMD_INPUT_CHUNK_TYPE_IMAGE ? "image" : "audio";
+    } else if (chunk_type == MTMD_INPUT_CHUNK_TYPE_IMAGE
+            || chunk_type == MTMD_INPUT_CHUNK_TYPE_VIDEO
+            || chunk_type == MTMD_INPUT_CHUNK_TYPE_AUDIO) {
+        const char * name = chunk_type == MTMD_INPUT_CHUNK_TYPE_IMAGE ? "image"
+                          : chunk_type == MTMD_INPUT_CHUNK_TYPE_VIDEO ? "video"
+                          : "audio";
         int64_t t0 = ggml_time_ms();
 
         LOG_INF("encoding %s slice...\n", name);
diff --git a/tools/mtmd/mtmd.cpp b/tools/mtmd/mtmd.cpp
index 5990778..bdb54ca 100644
--- a/tools/mtmd/mtmd.cpp
+++ b/tools/mtmd/mtmd.cpp
@@ -38,19 +38,22 @@ enum mtmd_pos_type {
     MTMD_POS_TYPE_NORMAL,    // number of positions equals to number of tokens
     MTMD_POS_TYPE_MROPE,     // qwen-vl mrope style, each image takes max(t,h,w) position indexes
     MTMD_POS_TYPE_HUNYUANVL, // HunyuanVL mrope + BOI/EOI/newline layout with XD-RoPE dim-3
+    MTMD_POS_TYPE_VIDEO,     // qwen-vl mrope style with per-super-frame temporal indices
 };
 
 struct mtmd_image_tokens {
     uint32_t nx; // number of tokens in x direction
     uint32_t ny; // number of tokens in y direction
+    uint32_t nt = 1; // number of temporal positions (1 for images, >1 for video)
     mtmd_pos_type pos = MTMD_POS_TYPE_NORMAL;
-    uint32_t image_idx = 0; // 0-based position of this image among image chunks in the prompt(used by pos == MTMD_POS_TYPE_HUNYUANVL)
+    uint32_t image_idx = 0; // 0-based position of this image among image chunks in the prompt (used by pos == MTMD_POS_TYPE_HUNYUANVL)
+    std::vector<int32_t> temporal_positions; // per-super-frame temporal indices (used by pos == MTMD_POS_TYPE_VIDEO)
     uint32_t n_tokens() const {
         if (pos == MTMD_POS_TYPE_HUNYUANVL) {
             // [BOI] [row0 tokens + newline] ... [row(ny-1) tokens + newline] [EOI]
             return (nx + 1) * ny + 2;
         }
-        return nx * ny;
+        return nx * ny * nt;
     }
     clip_image_f32_batch batch_f32; // preprocessed image patches
     std::string id; // optional user-defined ID, useful for KV cache tracking
@@ -59,8 +62,10 @@ struct mtmd_image_tokens {
         return mtmd_image_tokens{
             nx,
             ny,
+            nt,
             pos,
             image_idx,
+            temporal_positions,
             batch_f32.clone(),
             id
         };
@@ -549,7 +554,8 @@ struct mtmd_context {
 
     // get clip ctx based on chunk type
     clip_ctx * get_clip_ctx(const mtmd_input_chunk * chunk) const {
-        if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE) {
+        if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE
+                || chunk->type == MTMD_INPUT_CHUNK_TYPE_VIDEO) {
             return ctx_v;
         } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_AUDIO) {
             return ctx_a;
@@ -995,6 +1001,193 @@ struct mtmd_tokenizer {
     }
 };
 
+int32_t mtmd_tokenize_video(mtmd_context * ctx,
+            mtmd_input_chunks * output,
+            const mtmd_input_text * text,
+            const mtmd_bitmap ** bitmaps,
+            size_t n_bitmaps,
+            const int32_t * temporal_positions,
+            size_t n_temporal_positions) {
+    if (!ctx->ctx_v) {
+        LOG_ERR("%s: model does not support vision input\n", __func__);
+        return 1;
+    }
+    if (n_bitmaps < 2) {
+        LOG_ERR("%s: video requires at least 2 frames, got %zu\n", __func__, n_bitmaps);
+        return 1;
+    }
+
+    const size_t n_superframes = n_bitmaps / 2; // drop last frame if odd
+    if (n_temporal_positions != n_superframes) {
+        LOG_ERR("%s: temporal_positions count (%zu) must equal n_bitmaps/2 (%zu)\n",
+                __func__, n_temporal_positions, n_superframes);
+        return 1;
+    }
+
+    // Split text at media marker (legacy image_marker is no longer accepted upstream).
+    std::string input_text = text->text;
+
+    auto parts = mtmd_tokenizer::split_text(input_text, ctx->media_marker);
+    size_t n_markers = 0;
+    for (auto & p : parts) {
+        if (p == ctx->media_marker) n_markers++;
+    }
+    if (n_markers != 1) {
+        LOG_ERR("%s: video expects exactly 1 media marker, got %zu\n", __func__, n_markers);
+        return 1;
+    }
+
+    const llama_vocab * vocab = llama_model_get_vocab(ctx->text_model);
+
+    // Preprocess frame pairs into super-frames
+    clip_image_f32_batch video_batch;
+    video_batch.is_video = true;
+
+    for (size_t i = 0; i < n_superframes; i++) {
+        const mtmd_bitmap * bm_a = bitmaps[i * 2];
+        const mtmd_bitmap * bm_b = bitmaps[i * 2 + 1];
+
+        // Convert bitmaps to clip_image_u8
+        clip_image_u8_ptr img_a(clip_image_u8_init());
+        img_a->nx = bm_a->nx;
+        img_a->ny = bm_a->ny;
+        img_a->buf.resize(bm_a->data.size());
+        std::memcpy(img_a->buf.data(), bm_a->data.data(), bm_a->data.size());
+
+        clip_image_u8_ptr img_b(clip_image_u8_init());
+        img_b->nx = bm_b->nx;
+        img_b->ny = bm_b->ny;
+        img_b->buf.resize(bm_b->data.size());
+        std::memcpy(img_b->buf.data(), bm_b->data.data(), bm_b->data.size());
+
+        // Preprocess each frame (resize + normalize)
+        clip_image_f32_batch batch_a, batch_b;
+        if (!ctx->image_preproc->preprocess(*img_a, batch_a)) {
+            LOG_ERR("%s: failed to preprocess frame %zu\n", __func__, i * 2);
+            return 2;
+        }
+        if (!ctx->image_preproc->preprocess(*img_b, batch_b)) {
+            LOG_ERR("%s: failed to preprocess frame %zu\n", __func__, i * 2 + 1);
+            return 2;
+        }
+
+        // Both frames should have been resized to the same dimensions
+        auto & f32_a = batch_a.entries[0];
+        auto & f32_b = batch_b.entries[0];
+        GGML_ASSERT(f32_a->nx == f32_b->nx && f32_a->ny == f32_b->ny);
+
+        const int nx = f32_a->nx;
+        const int ny = f32_a->ny;
+        const int n_pixels = nx * ny;
+
+        // Merge two 3-channel HWC images into one 6-channel HWC super-frame
+        clip_image_f32_ptr super_frame(clip_image_f32_init());
+        super_frame->nx = nx;
+        super_frame->ny = ny;
+        super_frame->buf.resize(n_pixels * 6);
+
+        for (int p = 0; p < n_pixels; p++) {
+            super_frame->buf[p * 6 + 0] = f32_a->buf[p * 3 + 0];
+            super_frame->buf[p * 6 + 1] = f32_a->buf[p * 3 + 1];
+            super_frame->buf[p * 6 + 2] = f32_a->buf[p * 3 + 2];
+            super_frame->buf[p * 6 + 3] = f32_b->buf[p * 3 + 0];
+            super_frame->buf[p * 6 + 4] = f32_b->buf[p * 3 + 1];
+            super_frame->buf[p * 6 + 5] = f32_b->buf[p * 3 + 2];
+        }
+
+        video_batch.entries.push_back(std::move(super_frame));
+    }
+
+    // Create video image_tokens
+    mtmd_image_tokens_ptr image_tokens(new mtmd_image_tokens);
+    image_tokens->nx = clip_n_output_tokens_x(ctx->ctx_v, video_batch.entries[0].get());
+    image_tokens->ny = clip_n_output_tokens_y(ctx->ctx_v, video_batch.entries[0].get());
+    image_tokens->nt  = n_superframes;
+    image_tokens->pos = MTMD_POS_TYPE_VIDEO;
+    image_tokens->temporal_positions.assign(temporal_positions,
+                                             temporal_positions + n_temporal_positions);
+    image_tokens->batch_f32 = std::move(video_batch);
+    image_tokens->id = "video";
+
+    LOG_DBG("%s: video nx=%d ny=%d nt=%d n_tokens=%d\n", __func__,
+            image_tokens->nx, image_tokens->ny, image_tokens->nt,
+            image_tokens->n_tokens());
+
+    // Build output chunks: text + VIDEO + text
+    mtmd_input_chunks result;
+
+    auto add_text_chunk = [&](const std::vector<llama_token> & tokens) {
+        if (tokens.empty()) return;
+        if (!result.entries.empty()
+            && result.entries.back().type == MTMD_INPUT_CHUNK_TYPE_TEXT) {
+            auto & back = result.entries.back().tokens_text;
+            back.insert(back.end(), tokens.begin(), tokens.end());
+        } else {
+            result.entries.push_back({
+                MTMD_INPUT_CHUNK_TYPE_TEXT,
+                tokens,
+                nullptr,
+                nullptr,
+            });
+        }
+    };
+
+    for (auto & part : parts) {
+        if (part == ctx->media_marker) {
+            // Add vision begin token
+            if (!ctx->img_beg.empty()) {
+                auto tokens = mtmd_tokenizer::mtmd_tokenize_text_internal(
+                    vocab, ctx->img_beg, false, true);
+                add_text_chunk(tokens);
+            }
+
+            // Add VIDEO chunk
+            result.entries.push_back({
+                MTMD_INPUT_CHUNK_TYPE_VIDEO,
+                {},
+                std::move(image_tokens),
+                nullptr,
+            });
+
+            // Add vision end token
+            if (!ctx->img_end.empty()) {
+                auto tokens = mtmd_tokenizer::mtmd_tokenize_text_internal(
+                    vocab, ctx->img_end, false, true);
+                add_text_chunk(tokens);
+            }
+        } else {
+            auto tokens = mtmd_tokenizer::mtmd_tokenize_text_internal(
+                vocab, part, false, text->parse_special);
+            add_text_chunk(tokens);
+        }
+    }
+
+    // Handle BOS
+    if (text->add_special && llama_vocab_get_add_bos(vocab)) {
+        if (!result.entries.empty()
+            && result.entries[0].type == MTMD_INPUT_CHUNK_TYPE_TEXT) {
+            result.entries[0].tokens_text.insert(
+                result.entries[0].tokens_text.begin(), llama_vocab_bos(vocab));
+        } else {
+            mtmd_input_chunk bos_chunk{
+                MTMD_INPUT_CHUNK_TYPE_TEXT,
+                {llama_vocab_bos(vocab)},
+                nullptr,
+                nullptr,
+            };
+            result.entries.insert(result.entries.begin(), std::move(bos_chunk));
+        }
+    }
+
+    // Handle EOS
+    if (text->add_special && llama_vocab_get_add_eos(vocab)) {
+        add_text_chunk({llama_vocab_eos(vocab)});
+    }
+
+    *output = std::move(result);
+    return 0;
+}
+
 int32_t mtmd_tokenize(mtmd_context * ctx,
             mtmd_input_chunks * output,
             const mtmd_input_text * text,
@@ -1008,7 +1201,8 @@ int32_t mtmd_encode_chunk(mtmd_context * ctx, const mtmd_input_chunk * chunk) {
     if (chunk->type == MTMD_INPUT_CHUNK_TYPE_TEXT) {
         LOG_WRN("mtmd_encode_chunk has no effect for text chunks\n");
         return 0;
-    } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE) {
+    } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE
+            || chunk->type == MTMD_INPUT_CHUNK_TYPE_VIDEO) {
         if (!ctx->ctx_v) {
             LOG_ERR("%s: model does not support vision input\n", __func__);
             return 1;
@@ -1044,19 +1238,28 @@ int32_t mtmd_encode(mtmd_context * ctx, const mtmd_image_tokens * image_tokens)
     ctx->image_embd_v.resize(image_tokens->n_tokens() * n_mmproj_embd);
     bool ok = false;
 
+    const auto & entries = image_tokens->batch_f32.entries;
+
     if (clip_is_llava(ctx_clip)
         || clip_is_minicpmv(ctx_clip)
         || clip_is_glm(ctx_clip)
-        || proj_type == PROJECTOR_TYPE_INTERNVL) {
-        // TODO @ngxson : llava does not support batched encoding ; this should be fixed inside clip_image_batch_encode()
-        const auto & entries = image_tokens->batch_f32.entries;
+        || proj_type == PROJECTOR_TYPE_INTERNVL
+        || image_tokens->batch_f32.is_video) {
+        // Encode entries one at a time:
+        // - llava/minicpmv/glm: don't support batched encoding
+        // - video: clip_image_batch_encode only supports batch_size=1,
+        //          so we encode each super-frame individually
         for (size_t i = 0; i < entries.size(); i++) {
-            int n_tokens_per_image = clip_n_output_tokens(ctx_clip, entries[i].get());
+            int n_tokens_per_entry = clip_n_output_tokens(ctx_clip, entries[i].get());
             ok = clip_image_encode(
                 ctx_clip,
                 ctx->n_threads,
                 entries[i].get(),
-                ctx->image_embd_v.data() + i*n_mmproj_embd*n_tokens_per_image);
+                ctx->image_embd_v.data() + i*n_mmproj_embd*n_tokens_per_entry);
+            if (!ok) {
+                LOG_ERR("%s: failed to encode entry %zu/%zu\n", __func__, i, entries.size());
+                return 1;
+            }
         }
     } else {
         ok = clip_image_batch_encode(
@@ -1213,7 +1416,8 @@ const llama_token * mtmd_input_chunk_get_tokens_text(const mtmd_input_chunk * ch
 }
 
 const mtmd_image_tokens * mtmd_input_chunk_get_tokens_image(const mtmd_input_chunk * chunk) {
-    if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE) {
+    if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE
+            || chunk->type == MTMD_INPUT_CHUNK_TYPE_VIDEO) {
         return chunk->tokens_image.get();
     }
     return nullptr;
@@ -1222,7 +1426,8 @@ const mtmd_image_tokens * mtmd_input_chunk_get_tokens_image(const mtmd_input_chu
 size_t mtmd_input_chunk_get_n_tokens(const mtmd_input_chunk * chunk) {
     if (chunk->type == MTMD_INPUT_CHUNK_TYPE_TEXT) {
         return chunk->tokens_text.size();
-    } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE) {
+    } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE
+            || chunk->type == MTMD_INPUT_CHUNK_TYPE_VIDEO) {
         return mtmd_image_tokens_get_n_tokens(chunk->tokens_image.get());
     } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_AUDIO) {
         return chunk->tokens_audio->n_tokens;
@@ -1234,7 +1439,8 @@ size_t mtmd_input_chunk_get_n_tokens(const mtmd_input_chunk * chunk) {
 llama_pos mtmd_input_chunk_get_n_pos(const mtmd_input_chunk * chunk) {
     if (chunk->type == MTMD_INPUT_CHUNK_TYPE_TEXT) {
         return chunk->tokens_text.size();
-    } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE) {
+    } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE
+            || chunk->type == MTMD_INPUT_CHUNK_TYPE_VIDEO) {
         return mtmd_image_tokens_get_n_pos(chunk->tokens_image.get());
     } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_AUDIO) {
         return chunk->tokens_audio->n_tokens;
@@ -1244,7 +1450,8 @@ llama_pos mtmd_input_chunk_get_n_pos(const mtmd_input_chunk * chunk) {
 }
 
 const char * mtmd_input_chunk_get_id(const mtmd_input_chunk * chunk) {
-    if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE) {
+    if (chunk->type == MTMD_INPUT_CHUNK_TYPE_IMAGE
+            || chunk->type == MTMD_INPUT_CHUNK_TYPE_VIDEO) {
         return chunk->tokens_image->id.c_str();
     } else if (chunk->type == MTMD_INPUT_CHUNK_TYPE_AUDIO) {
         return chunk->tokens_audio->id.c_str();
@@ -1292,6 +1499,21 @@ size_t mtmd_image_tokens_get_ny(const mtmd_image_tokens * image_tokens) {
     return image_tokens->ny;
 }
 
+size_t mtmd_image_tokens_get_nt(const mtmd_image_tokens * image_tokens) {
+    return image_tokens->nt;
+}
+
+const int32_t * mtmd_image_tokens_get_temporal_positions(
+        const mtmd_image_tokens * image_tokens,
+        size_t * n_positions) {
+    if (image_tokens->temporal_positions.empty()) {
+        *n_positions = 0;
+        return nullptr;
+    }
+    *n_positions = image_tokens->temporal_positions.size();
+    return image_tokens->temporal_positions.data();
+}
+
 mtmd_decoder_pos mtmd_image_tokens_get_decoder_pos(const mtmd_image_tokens * image_tokens, llama_pos pos_0, size_t i) {
     mtmd_decoder_pos pos;
     switch (image_tokens->pos) {
@@ -1302,6 +1524,20 @@ mtmd_decoder_pos mtmd_image_tokens_get_decoder_pos(const mtmd_image_tokens * ima
                 pos.y = pos_0 + (i / image_tokens->nx);
                 pos.z = 0; // unused for now
             } break;
+        case MTMD_POS_TYPE_VIDEO:
+            {
+                // tokens are laid out as nt super-frames, each of nx*ny tokens.
+                // each super-frame's temporal index comes from temporal_positions;
+                // spatial coords mirror the MROPE case within a frame.
+                const uint32_t per_frame = image_tokens->nx * image_tokens->ny;
+                const size_t   frame_idx = i / per_frame;
+                const size_t   in_frame  = i % per_frame;
+                const int32_t  t_offset  = image_tokens->temporal_positions[frame_idx];
+                pos.t = pos_0 + t_offset;
+                pos.x = pos_0 + (in_frame % image_tokens->nx);
+                pos.y = pos_0 + (in_frame / image_tokens->nx);
+                pos.z = 0;
+            } break;
         case MTMD_POS_TYPE_NORMAL:
             {
                 pos.t = pos_0 + i;
@@ -1355,6 +1591,10 @@ llama_pos mtmd_image_tokens_get_n_pos(const mtmd_image_tokens * image_tokens) {
     switch (image_tokens->pos) {
         case MTMD_POS_TYPE_MROPE:
             return std::max(image_tokens->nx, image_tokens->ny);
+        case MTMD_POS_TYPE_VIDEO:
+            // sequential dim advances by max({nt, nx, ny}); temporal_positions
+            // place each super-frame's temporal index within that span.
+            return std::max({image_tokens->nt, image_tokens->nx, image_tokens->ny});
         case MTMD_POS_TYPE_NORMAL:
             return image_tokens->n_tokens();
         case MTMD_POS_TYPE_HUNYUANVL:
diff --git a/tools/mtmd/mtmd.h b/tools/mtmd/mtmd.h
index e364174..b0c6892 100644
--- a/tools/mtmd/mtmd.h
+++ b/tools/mtmd/mtmd.h
@@ -54,6 +54,7 @@ enum mtmd_input_chunk_type {
     MTMD_INPUT_CHUNK_TYPE_TEXT,
     MTMD_INPUT_CHUNK_TYPE_IMAGE,
     MTMD_INPUT_CHUNK_TYPE_AUDIO,
+    MTMD_INPUT_CHUNK_TYPE_VIDEO,
 };
 
 // opaque types
@@ -183,6 +184,12 @@ MTMD_API void               mtmd_input_chunk_free(mtmd_input_chunk * chunk);
 // the instance will be constructed via mtmd_tokenize()
 // it will be freed along with mtmd_input_chunk
 MTMD_API size_t       mtmd_image_tokens_get_n_tokens(const mtmd_image_tokens * image_tokens); // TODO: deprecate
+MTMD_API size_t       mtmd_image_tokens_get_nt      (const mtmd_image_tokens * image_tokens); // temporal positions (1 for images, >1 for video)
+// get temporal positions array for video chunks (nullptr for images)
+// n_positions will be set to the number of temporal positions
+MTMD_API const int32_t * mtmd_image_tokens_get_temporal_positions(
+    const mtmd_image_tokens * image_tokens,
+    size_t * n_positions);
 MTMD_API const char * mtmd_image_tokens_get_id      (const mtmd_image_tokens * image_tokens); // TODO: deprecate
 // number of temporal positions (equals to max(t,h,w) for M-RoPE; equals to n_tokens otherwise)
 MTMD_API llama_pos    mtmd_image_tokens_get_n_pos   (const mtmd_image_tokens * image_tokens); // TODO: deprecate
@@ -204,6 +211,23 @@ struct mtmd_decoder_pos {
 // return relative position (for example, embedding 0 will have position (0, 0, 0); remember to adjust it to the current absolute position)
 MTMD_API struct mtmd_decoder_pos mtmd_image_tokens_get_decoder_pos(const mtmd_image_tokens * image_tokens, llama_pos pos_0, size_t i);
 
+// tokenize a video prompt: pairs consecutive bitmaps into 6-channel super-frames
+// and creates a single VIDEO chunk with temporal position encoding
+// n_bitmaps must be >= 2; odd counts drop the last frame
+// n_temporal_positions must equal n_bitmaps / 2
+// the prompt must have exactly one media marker
+// return values:
+//   0 on success
+//   1 on input validation error
+//   2 on preprocessing error
+MTMD_API int32_t mtmd_tokenize_video(mtmd_context * ctx,
+                                      mtmd_input_chunks * output,
+                                      const mtmd_input_text * text,
+                                      const mtmd_bitmap ** bitmaps,
+                                      size_t n_bitmaps,
+                                      const int32_t * temporal_positions,
+                                      size_t n_temporal_positions);
+
 // tokenize an input text prompt and a list of bitmaps (images/audio)
 // the prompt must have the input image marker (default: "<__media__>") in it
 // the default marker is defined by mtmd_default_marker()
diff --git a/tools/server/server-common.cpp b/tools/server/server-common.cpp
index 21c843c..19321a0 100644
--- a/tools/server/server-common.cpp
+++ b/tools/server/server-common.cpp
@@ -353,7 +353,7 @@ void server_tokens::push_back(llama_token tok) {
 
 void server_tokens::push_back(const mtmd_input_chunk * chunk) {
     auto type = mtmd_input_chunk_get_type(chunk);
-    if (type == MTMD_INPUT_CHUNK_TYPE_IMAGE || type == MTMD_INPUT_CHUNK_TYPE_AUDIO) {
+    if (type == MTMD_INPUT_CHUNK_TYPE_IMAGE || type == MTMD_INPUT_CHUNK_TYPE_AUDIO || type == MTMD_INPUT_CHUNK_TYPE_VIDEO) {
         GGML_ASSERT(has_mtmd);
         const size_t n_tokens = mtmd_input_chunk_get_n_tokens(chunk);
         size_t start_idx = tokens.size();
@@ -539,8 +539,10 @@ int32_t server_tokens::process_chunk(
             int32_t seq_id,
             size_t & n_tokens_out) const {
     const auto & chunk = find_chunk(idx);
-    const char * name = mtmd_input_chunk_get_type(chunk.get()) == MTMD_INPUT_CHUNK_TYPE_IMAGE
-                        ? "image" : "audio";
+    auto media_type = mtmd_input_chunk_get_type(chunk.get());
+    const char * name = media_type == MTMD_INPUT_CHUNK_TYPE_IMAGE ? "image"
+                      : media_type == MTMD_INPUT_CHUNK_TYPE_VIDEO ? "video"
+                      : "audio";
     SRV_INF("processing %s...\n", name);
     int32_t n_batch = llama_n_batch(ctx);
     int64_t t0 = ggml_time_ms();
@@ -713,7 +715,7 @@ static std::string fnv_hash(const uint8_t * data, size_t len) {
     return std::to_string(hash);
 }
 
-server_tokens process_mtmd_prompt(mtmd_context * mctx, std::string prompt, std::vector<raw_buffer> files) {
+server_tokens process_mtmd_prompt(mtmd_context * mctx, std::string prompt, std::vector<raw_buffer> files, const json & mm_kwargs) {
     mtmd::bitmaps bitmaps;
     for (auto & file : files) {
         mtmd::bitmap bmp(mtmd_helper_bitmap_init_from_buf(mctx, file.data(), file.size()));
@@ -735,13 +737,57 @@ server_tokens process_mtmd_prompt(mtmd_context * mctx, std::string prompt, std::
     };
     mtmd::input_chunks chunks(mtmd_input_chunks_init());
     auto bitmaps_c_ptr = bitmaps.c_ptr();
-    int32_t tokenized = mtmd_tokenize(mctx,
-                                      chunks.ptr.get(),
-                                      &inp_txt,
-                                      bitmaps_c_ptr.data(),
-                                      bitmaps_c_ptr.size());
-    if (tokenized != 0) {
-        throw std::runtime_error("Failed to tokenize prompt");
+
+    // Detect video mode
+    bool is_video = json_value(mm_kwargs, "is_video", false);
+
+    if (is_video) {
+        // In video mode, multiple image_url entries each created a media marker.
+        // Collapse all markers into a single one for mtmd_tokenize_video.
+        // Use get_media_marker() — the actual (per-server, randomized) marker
+        // the server substituted into the prompt — not mtmd_default_marker(),
+        // which is the hardcoded default and won't match on a live server.
+        std::string marker = get_media_marker();
+        size_t first_pos = prompt.find(marker);
+        if (first_pos != std::string::npos) {
+            size_t pos = first_pos + marker.size();
+            while ((pos = prompt.find(marker, pos)) != std::string::npos) {
+                prompt.erase(pos, marker.size());
+            }
+        }
+
+        // Extract temporal positions
+        auto tp_json = json_value(mm_kwargs, "temporal_positions", json::array());
+        std::vector<int32_t> temporal_positions;
+        for (auto & tp : tp_json) {
+            temporal_positions.push_back(tp.get<int32_t>());
+        }
+
+        // Re-create input text with collapsed prompt
+        mtmd_input_text inp_txt_video = {
+            prompt.c_str(),
+            /* add_special */   true,
+            /* parse_special */ true,
+        };
+        int32_t tokenized = mtmd_tokenize_video(mctx,
+                                                 chunks.ptr.get(),
+                                                 &inp_txt_video,
+                                                 bitmaps_c_ptr.data(),
+                                                 bitmaps_c_ptr.size(),
+                                                 temporal_positions.data(),
+                                                 temporal_positions.size());
+        if (tokenized != 0) {
+            throw std::runtime_error("Failed to tokenize video prompt");
+        }
+    } else {
+        int32_t tokenized = mtmd_tokenize(mctx,
+                                           chunks.ptr.get(),
+                                           &inp_txt,
+                                           bitmaps_c_ptr.data(),
+                                           bitmaps_c_ptr.size());
+        if (tokenized != 0) {
+            throw std::runtime_error("Failed to tokenize prompt");
+        }
     }
     auto result = server_tokens(chunks, true);
     return result;
@@ -1155,6 +1201,11 @@ json oaicompat_chat_params_parse(
         throw std::invalid_argument("top_logprobs requires logprobs to be set to true");
     }
 
+    // Pass through multimodal processor kwargs (used for video mode)
+    if (body.contains("mm_processor_kwargs")) {
+        llama_params["mm_processor_kwargs"] = body.at("mm_processor_kwargs");
+    }
+
     // Copy remaining properties to llama_params
     // This allows user to use llama.cpp-specific params like "mirostat", ... via OAI endpoint.
     // See "launch_slot_with_task()" for a complete list of params supported by llama.cpp
diff --git a/tools/server/server-common.h b/tools/server/server-common.h
index 4681f9c..98b1830 100644
--- a/tools/server/server-common.h
+++ b/tools/server/server-common.h
@@ -256,7 +256,7 @@ llama_tokens tokenize_mixed(const llama_vocab * vocab, const json & json_prompt,
 size_t validate_utf8(const std::string& text);
 
 // process mtmd prompt, return the server_tokens containing both text tokens and media chunks
-server_tokens process_mtmd_prompt(mtmd_context * mctx, std::string prompt, std::vector<raw_buffer> files);
+server_tokens process_mtmd_prompt(mtmd_context * mctx, std::string prompt, std::vector<raw_buffer> files, const json & mm_kwargs = json::object());
 
 /**
  * break the input "prompt" object into multiple prompt if needed, then tokenize them
diff --git a/tools/server/server-context.cpp b/tools/server/server-context.cpp
index 08ff1e3..bd0de46 100644
--- a/tools/server/server-context.cpp
+++ b/tools/server/server-context.cpp
@@ -3195,7 +3195,8 @@ std::unique_ptr<server_res_generator> server_routes::handle_completions_impl(
 
         if (res_type != TASK_RESPONSE_TYPE_NONE && ctx_server.mctx != nullptr) {
             // This is the case used by OAI compatible chat path with MTMD. TODO It can be moved to the path below.
-            inputs.push_back(process_mtmd_prompt(ctx_server.mctx, prompt.get<std::string>(), files));
+            json mm_kwargs = json_value(data, "mm_processor_kwargs", json::object());
+            inputs.push_back(process_mtmd_prompt(ctx_server.mctx, prompt.get<std::string>(), files, mm_kwargs));
         } else {
             // Everything else, including multimodal completions.
             inputs = tokenize_input_prompts(ctx_server.vocab, ctx_server.mctx, prompt, true, true);
